
  <div class="container">
    <h1>Log Aplication</h1>
        <form method="post" action="/log" >
            <div class="row">
                <div class="col-md-3 mt-1 mb-1" >    
                    <input type="text" class="form-control" name="cari">
                </div>  
                <div class="col-md-3 mt-1">
                    <button type="submit" class="btn btn-primary d-block"><i class="bi bi-search"></i> Cari</button>
                </div>
            </div> 
        </form>
    <div class="row">
        <div class="col">

            <!-- menampilkan pesan -->
            <% if (msg.length !== 0) { %>
                <div class="alert alert-success" role="alert"> <%= msg %> </div>
            <% } %>

            <% if (msg2.length !== 0) { %>
                <div class="alert alert-warning" role="alert"> <%= msg2 %> </div>
            <% } %>

            <table class="table table-striped table-hover  mt-3">
                <thead class="table-primary">
                    <tr>
                        <th>No</th>
                        <th>Nama</th>
                        <th>Url</th>
                        <th>Role</th>
                        <th>Tanggal</th>
                        <th>Waktu</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (cont == null) { %>
                        <tr><td colspan="7"><center><b>Log dari website ini masih kosong </b></center></td></tr>  
                    <% }else if (cont.length == 0) { %>
                        <tr><td colspan="7"><center><b>Log website ini masih kosong</b></center></td></tr>
                    <% }else{ %>  
                        <% cont.forEach( (con,index) => { %>
                            <tr>
                                <td><%= index+1 %></td>
                                <td><%= con.tgl.toLocaleDateString('id-ID') %> </td>
                                <td><%= con.name %> </td>
                                <td><%= con.jam_masuk %> </td>                    
                                <td><%= con.jam_keluar %> </td>                    
                            </tr>
                        <% }) %>
                    <% } %> 
                </tbody>
            </table>
               
        </div>
    </div>
</div>

